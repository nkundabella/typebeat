PORT=5000
DATABASE_URL=postgresql://<username>:<password>@localhost:5432/postgres
# Change "postgres" at the end of the URL to point to a specific database if desired, 
# but the script will automatically create a "typebeat" database and connect to it.
